<table>
  <% allTodos.forEach(todo => { %>
    <tr>
      <td><%= todo.title %></td>
      <td><%= todo.dueDate %></td>
      <td><%= todo.completed ? "✅" : "❌" %></td>
    </tr>
  <% }); %>
</table>

