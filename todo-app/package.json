{
  "name": "todo_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prepare": "[ \"$NODE_ENV\" != \"production\" ] && husky install || true",
    "migrate": "npx sequelize-cli db:migrate",
    "start": "npm run migrate && node index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.8",
    "sequelize": "^6.32.1",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "husky": "^8.0.3",
    "sequelize-cli": "^6.6.0"
  }
}
